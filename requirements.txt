# No third-party packages — the build uses only the Python standard library.
# (Listed here so the GitHub Action's "pip install -r requirements.txt" step
#  is happy and there's a place to add dependencies later.)
